export enum ColorRoute {
  ['road'] = '',
  ['mtb'] = '',
  ['gravel'] = '',
}
export enum EColor {
  default = 'default',
  primary = 'primary',
  secondary = 'secondary',
  error = 'error',
  info = 'info',
  success = 'success',
  warning = 'warning',
}

export type TColors =
  | 'default'
  | 'primary'
  | 'secondary'
  | 'error'
  | 'info'
  | 'success'
  | 'warning';
